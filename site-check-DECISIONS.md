# 設計判断について（DECISIONS）

このドキュメントは、READMEには書ききれない「なぜその技術・設計を選んだか」をまとめたものです。作った本人が、判断の理由と、そのトレードオフをどう認識しているかを共有する目的で書いています。

---

## 1. バックエンドにFastAPIを選んだ理由

型を明示したリクエスト/レスポンス定義が、そのままAPIドキュメント（`/docs`）として自動生成される点を重視しました。バックエンド経験がまだ浅い中で、書いたコードの結果がすぐドキュメントという形でフィードバックされることは、学習効率の面でも開発効率の面でも大きなメリットでした。

比較検討した選択肢:
- **Flask**: 自由度は高いものの、型定義やバリデーションを自前で組む必要があり、学習初期の当時としては遠回りだと判断しました。
- **Django**: フルスタックフレームワークで、今回の規模（単機能のAPI）には過剰だと判断しました。

## 2. データベースにSQLiteを選んだ理由

個人開発の最初のフェーズでは、まず確実に動くことを優先しました。SQLiteはファイル1つで完結するため、接続設定まわりのトラブルが起きにくいという利点があります。

一方で、将来のスケールも見据え、`DATABASE_URL`という環境変数1つでSQLAlchemyの接続先を切り替えられる構成にしてあります。アクセスが増えた場合は、コードを変更せずにTursoやPostgreSQLへ移行できます。「今SQLiteを選んだこと」が将来の技術的負債にならないよう、あらかじめ設計しています。

## 3. フロントエンドとバックエンドを別ホスティングに分けた理由

フロントエンド（React）はNetlify、バックエンド（FastAPI）はRenderと、別々にホスティングしています。

静的ファイル配信とPythonの実行環境では、そもそも必要なインフラの形が異なります。それぞれに適したサービスを組み合わせる方が、単一のサーバーに無理に同居させるより構成がシンプルになると判断しました。実務でもフロントエンドとバックエンドが別チーム・別リポジトリで管理されるケースは多く、その分離に慣れておく狙いもありました。

この構成で発生する課題として、CORSの設定管理が挙げられます。現状は開発中につき全オリジンを許可していますが、本番運用する場合はフロントエンドの実ドメインのみに絞る必要があると認識しています。

## 4. PDF生成ライブラリの転換（WeasyPrint → reportlab）

開発初期はWeasyPrint（HTML/CSSからPDFを生成できるライブラリ）を採用し、ローカル環境では問題なく動作していました。しかし実際にRenderへデプロイしたところ、WeasyPrintが依存するPango・CairoといったOSレベルのライブラリがRenderの標準環境に存在せず、ビルドが失敗しました。

「ローカルで動くこと」と「本番環境で動くこと」は別問題であり、Pythonのライブラリにはpipで完結するものと、OS側のシステムライブラリを必要とするものの2種類があるということを、このトラブルを通じて学びました。

最終的に、システムライブラリに依存しないpure PythonのreportlabにPDF生成を切り替えました。HTML/CSSでレイアウトを組めなくなる代わりに、Pythonコードで直接レイアウトを制御できるようになり、結果的に出力の細部までコントロールしやすくなりました。

## 5. 日本語フォントをサブセット化して埋め込んだ理由

PDFで日本語を正しく表示するには、フォントをPDFファイルに埋め込む必要があります。Noto Sans JPのフルセットは約9.5MBあり、GitHub経由での手動アップロードでは何度か転送が不完全に終わり、ファイルが破損する問題が起きました。

そこで、レポート内で実際に使用する文字（見出し・ラベル・数字・記号など）だけを`fonttools`で抽出し、約60KBまで圧縮したフォントを作成して埋め込みました。動的に生成される診断コメントの文言が増えるたびに、文字セットを再計算してフォントを作り直す運用にしています。

## 6. 改善提案（Recommendations）機能をルールベースにした理由

SEO・セキュリティの診断項目（title・meta description・h1・alt属性・HTTPS化など）は、何が推奨されるかがベストプラクティスとしてすでに明確に定義されています。AIによる判定を挟まずとも、条件分岐によるルールベースの実装で十分な精度が出ると判断しました。

ルールベースであることには、提案内容の根拠を常に100%説明できるという利点もあります。

## 7. 前回結果との差分表示の設計

診断結果をSQLiteに保存し、同じURLが再診断された際に直近の過去レコードを1件取得してスコアを比較する設計にしています。差分計算のロジックをバックエンド側に持たせることで、フロントエンドは受け取った差分値をそのまま表示するだけのシンプルな実装で済んでいます。

---

## 想定される質問への回答

**Q. なぜテストコードを書いていないのですか？**
個人開発の初期フェーズでは手動確認を優先しましたが、入出力が明確な診断ロジック（`diagnostics.py`）部分から、pytestによるユニットテストを追加することを次のステップとして考えています。

**Q. アクセスが急増した場合はどうなりますか？**
現状はRenderの無料インスタンス1台構成で、スケールを前提とした設計にはなっていません。SQLiteの書き込み制限も踏まえると、アクセス増加が見込まれる場合はPostgreSQLへの移行とインスタンスのスケールアップが必要になると認識しています。
